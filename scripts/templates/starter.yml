name: tasks
on:
  push:
    branches:
      - dist
jobs: 
  prepare:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v2
      - name: prep
        run: |
          mkdir tmp
          git add tmp
          git commit -m "Create tmp folder"
          git push origin dist
